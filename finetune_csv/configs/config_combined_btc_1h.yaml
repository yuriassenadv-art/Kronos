# Auto-generated by infra/build_training_dataset.py
# Strategy: domain adaptation — Binance (long history) + Hyperliquid (target exchange)
# Binance rows prior to the HL window are prepended for broad pattern coverage;
# Hyperliquid rows cover the recent period with exact target-exchange prices.
#
# To retrain: python3 -m infra.build_training_dataset --symbol BTC --interval 1h
#             python3 finetune_csv/train_sequential.py --config finetune_csv/configs/config_combined_btc_1h.yaml

data:
  clip: 5.0
  data_path: finetune_csv/data/COMBINED_BTC_1h.csv
  lookback_window: 512
  max_context: 512
  predict_window: 24
  test_ratio: 0.05
  train_ratio: 0.85
  val_ratio: 0.1
device:
  device_id: 0
  use_cuda: true
experiment:
  description: Kronos fine-tuned on combined Binance+Hyperliquid BTC 1h dataset (domain
    adaptation)
  name: kronos_combined_btc_1h
  pre_trained_predictor: true
  pre_trained_tokenizer: true
  skip_existing: false
  train_basemodel: true
  train_tokenizer: true
  use_comet: false
model_paths:
  base_path: finetune_csv/finetuned
  base_save_path: ''
  basemodel_save_name: basemodel
  exp_name: COMBINED_BTC_1h
  finetuned_tokenizer: ''
  pretrained_predictor: NeoQuasar/Kronos-small
  pretrained_tokenizer: NeoQuasar/Kronos-Tokenizer-base
  tokenizer_save_name: tokenizer
training:
  accumulation_steps: 2
  adam_beta1: 0.9
  adam_beta2: 0.95
  adam_weight_decay: 0.1
  basemodel_epochs: 20
  batch_size: 32
  log_interval: 50
  num_workers: 4
  predictor_learning_rate: 1.0e-06
  seed: 42
  tokenizer_epochs: 30
  tokenizer_learning_rate: 0.0002
